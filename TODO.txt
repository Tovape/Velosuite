Todo:
Add background image load in the background and at next page reload take that image instead of the min compressed one, like a global cache function, check if there is size enough to store stuff like fonts (max priority), images etc
Add showable sidebar that shows links you go most to (like bookmarks, for example the Main excel sheet)
